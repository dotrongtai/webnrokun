<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Đăng ký tài khoản NRO</title>

  <style>
    * { box-sizing: border-box; }

    body {
      margin: 0;
      font-family: Arial, sans-serif;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      background: radial-gradient(circle at top, #1b1b1b 0%, #0a0a0a 70%);
      color: #fff;
    }

    .container {
      width: 100%;
      padding: 20px;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .box {
      width: 100%;
      max-width: 420px;
      background: rgba(30, 30, 30, 0.95);
      border-radius: 16px;
      padding: 28px 26px;
      box-shadow: 0 8px 30px rgba(0,0,0,0.6);
      border: 1px solid rgba(255,255,255,0.08);
      backdrop-filter: blur(8px);
    }

    .box h2 {
      text-align: center;
      margin: 0 0 18px;
      font-size: 24px;
      color: #00ff66;
      letter-spacing: 0.5px;
    }

    .sub {
      text-align: center;
      font-size: 13px;
      color: #bdbdbd;
      margin-bottom: 22px;
      line-height: 1.4;
    }

    .form-group {
      margin-bottom: 14px;
    }

    label {
      font-size: 13px;
      color: #cfcfcf;
      display: block;
      margin-bottom: 6px;
    }

    input {
      width: 100%;
      padding: 12px 14px;
      border-radius: 10px;
      border: 1px solid rgba(255,255,255,0.12);
      background: #232323;
      color: #fff;
      outline: none;
      font-size: 14px;
      transition: 0.2s;
    }

    input:focus {
      border-color: #00ff66;
      box-shadow: 0 0 0 3px rgba(0, 255, 102, 0.12);
    }

    button {
      width: 100%;
      padding: 12px 14px;
      margin-top: 6px;
      border: none;
      border-radius: 10px;
      background: linear-gradient(135deg, #00ff66, #00cc55);
      color: #000;
      font-weight: bold;
      cursor: pointer;
      font-size: 15px;
      transition: 0.2s;
      box-shadow: 0 8px 20px rgba(0, 255, 102, 0.18);
    }

    button:hover {
      transform: translateY(-1px);
      filter: brightness(1.05);
    }

    button:active {
      transform: translateY(0px);
      filter: brightness(0.95);
    }

    .msg {
      margin-top: 16px;
      padding: 12px 14px;
      border-radius: 10px;
      text-align: center;
      font-weight: bold;
      background: rgba(255, 255, 0, 0.08);
      border: 1px solid rgba(255, 255, 0, 0.15);
      color: #ffeb3b;
      font-size: 14px;
    }

    .footer {
      margin-top: 18px;
      text-align: center;
      font-size: 12px;
      color: #888;
    }

    .footer a {
      color: #00ff66;
      text-decoration: none;
    }

    .footer a:hover {
      text-decoration: underline;
    }

    /* Mobile tối ưu */
    @media (max-width: 420px) {
      .box {
        padding: 22px 18px;
      }
      .box h2 {
        font-size: 21px;
      }
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="box">
      <h2>Đăng ký NRO</h2>
      <div class="sub">
        Tạo tài khoản để đăng nhập vào game.<br>
        Nhập thông tin chính xác để tránh mất tài khoản.
      </div>

      <form action="/register" method="POST">
        <div class="form-group">
          <label>Tên tài khoản</label>
          <input type="text" name="username" placeholder="Ví dụ: taideptrai1234"
                 required minlength="3" maxlength="20" autocomplete="username">
        </div>

        <div class="form-group">
          <label>Mật khẩu</label>
          <input type="password" name="password" placeholder="Nhập mật khẩu"
                 required minlength="5" maxlength="20" autocomplete="new-password">
        </div>

        <div class="form-group">
          <label>Nhập lại mật khẩu</label>
          <input type="password" name="repassword" placeholder="Nhập lại mật khẩu"
                 required minlength="5" maxlength="20" autocomplete="new-password">
        </div>

        <button type="submit">Đăng ký</button>
      </form>

      <% if (msg) { %>
        <div class="msg"><%= msg %></div>
      <% } %>

      <div class="footer">
        © Web NRO - Đăng ký tài khoản | <a href="#">Tải game</a>
      </div>
    </div>
  </div>
</body>
</html>
